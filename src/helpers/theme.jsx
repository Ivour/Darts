import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#69a2eb",
    },
    secondary: {
      main: "#69e3eb",
    },

    warning: {
      main: "#efba7d",
    },
  },
});

export default theme;
